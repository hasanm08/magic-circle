# magic-circle
a tiny mobile app that fetches a random image from our API and displays it centered as a square. A button should fetch a new image. The background should adapt to the image’s colors for an immersive effect.
